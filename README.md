# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные
В этом разделе будет представлена информация о моделях данных  приложения.

### Модель каталога товаров

#### Products

Данная модель представляет собой список всех существующих товаров. Используется для отображения всех товаров на главной странице.

#### Поля класса:

  private items: IProduct[] = [];
    Массив товаров - список всех существующих товаров, который мы получаем с сервера

  private selectedItem: IProduct | null = null;
    Выбранная карточка - карточка, которую выбрал пользователь

#### Методы класса:
  
  getItems - получение списка всех существующих товаров

  getItemById - получение товара по его ID

  saveSelectedItem - сохранение данных о выбраной карточки
    
  getSelectedItem - получить данные выбранной карточки
  

### Модель корзины

#### Cart

#### Поля класса:

  items: IProduct[] = []; - все товары в корзине

#### Методы класса:

  getItems - получение всех товаров в корзне
    
  addItem - для добавления товара в корзину

  removeItem - для удаления товара из корзины

  clearCart - очистка корзины

  getTotalPrice - подсчет общей цены товаров в корзине

  getItemsQuantity - получение количества товаров в корзине

  checkItem - проверка наличия товара в корзине


### Модель покупателя

export class Buyer {

#### Поля класса:

data: IBuyer = {
        payment: "null", - способ оплаты, по умолчанию null
        email: "", - адрес электронной почты
        phone: "", - номер телефона
        address: "" - адрес
    };

#### Методы класса:

  updateField - внесение изменений в данные пользователя

  getAllData -получение данных о пользователе

  clearAll - отчистка данных



### Слой коммутации

Для взаимодействия с сервером используется класс ProductOrderService.

Класс включает в себя два метода:

#### метод подучения списка товаров

данный метод использует GET запрос на сервер для получения списка всех товаров

async getProducts(): Promise<IProductListResponse> {
    return this.api.get<IProductListResponse>('/product/');
  }

#### метод создания заказа

данный метод использует POST запрос на сервер для создания заказа

async createOrder(orderData: IOrderRequest): Promise<IOrderResponse> {
    return this.api.post<IOrderResponse>('/order/', orderData);
  }

### класс ProductOrderService

export class ProductOrderService {
  private api: Api;

  constructor(baseUrl: string) {
    this.api = new Api(baseUrl);
  }

  //Получает список товаров с сервера

  async getProducts(): Promise<IProductListResponse> {
    return this.api.get<IProductListResponse>('/product/');
  }

  //Создаёт заказ на товары

  async createOrder(orderData: IOrderRequest): Promise<IOrderResponse> {
    return this.api.post<IOrderResponse>('/order/', orderData);
  }
}


### Слой Представления
Слой Представления является частью проекта, который отвечает за визуальное отображение данных. Все классы представления являются потомками класса `Component`.


#### Интерфейс IHeader:
Поля интерфейса:
`counter: number`- данные о количестве товаров в корзине, которые отображаются в хэдере.

#### Класс Header
Класс описывает элемент хэдера страницы с значком корзины с счетчиком.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - конструктор принимает в качестве контейнера HTMLElement с классом `header` и событие для кнопки элемента корзины. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов в разметке и устанавливается слушатель события клика на кнопку элемента корзины, в котором генерируется событие открытия корзины.

Поля класса:
`counterElement: HTMLElement` - хранит элемент счетчика товаров в корзине
`protected basketButton: HTMLButtonElement` - хранит кнопку элемента корзины, при нажатии на которую открывается модальное окно с содержимым корзины внутри

Методы класса:
`set counter(value: number)` - используется для установки количества товаров в корзине для отображения в хэдере


#### Интерфейс IGallery:
Поля интерфейса:
`catalog: HTMLElement[]` - элемент каталога

#### Класс Gallery
Класс каталог с карточками товарами внутри.

Конструктор:
`constructor(container: HTMLElement)` - конструктор в качестве контейнера принимает HTMLElement с классом `gallery`. Внутри вызывается родительский конструктор, ищет необходимый элемент в разметке.

Поля класса:
`catalogElement: HTMLElement` - хранит элемент списка товаров

Методы класса:
`set catalog(items: HTMLElement[])` - добавить товары в список, принимает массив карточек товаров.


#### Интерфейс IModalData:
Поля интерфейса:
`modalContent: HTMLElement` - элемент модального окна

#### Класс Modal
Класс описывает модальные окна в веб ларьке.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - конструктор в качестве контейнера принимает HTMLElement с классом `modal__container` и событие закрытия окна. Внутри вызывается родительский конструктор, происходит поиск необходимых элементов в разметке и устанавливается слушатель события клика на кнопку и контейнер для закрытия модального окна, которые генерируют событие закрытия модального окна.

Поля класса:
`closeButton: HTMLButtonElement` - хранит кнопку закрытия модального окна
`modalContentContainer: HTMLElement` - хранит содержимое элемента модального окна
`protected openFlag: boolean = false` - состояние модального окна: открыто или закрыто

Методы класса:
`set modalContent(content: HTMLElement)` - установление содержимого модального окна
`open()`- если окно не открыто, то меняет значение `openFlag` и добавляет класс `modal_active` к контейнеру
`close()` - если окно открыто, то меняет значение `openFlag` и удаляет класс `modal_active` у контейнеру
`isOpen(): boolean` - возвращает значение `openFlag` 


#### Интерфейс ISuccess:
Поля интерфейса:
`totalAmount: number` - полную сумма совершенной покупки

#### Класс Success
Класс для окна, появляющегося при успешном совершении заказа. 

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - конструктор в качестве контейнера принимает HTMLElement с классом `order-success` и событие для кнопки закрытия контейнера и перехода к новым покупкам. Внутри вызывается родительский конструктор, происходит поиск нужных элементов и устанавливается слушатель события клика на кнопку, который генерирует событие закрытия модального окна.

Поля класса:
`protected successDescription: HTMLElement` - хранит общую стоимость купленных товаров
`protected successButton: HTMLButtonElement` - хранит кнопку для закрытия контейнера и перехода к новым покупкам


Методы класса:
`set totalAmount(value: number)` - метод для указания общей стоимости товаров


#### Компоненты карточки товара
Карточка товара имеет три вида отображения на сайте: в каталоге, полная карточка в модальном окне, в корзине товаров. Для всех трех видов отобрадения описаны классы, которые имеют общего родителя - класс `Card`. 

#### Интерфейс ICard
Поля интерфейса:
`ig: string` - идентификатор товара
`title: string` - название товара
`price: number | null` - цена товара

#### Класс Card
Класс описывает базовый функционал карточки товара. Базовая карточка содержит идетификатор, название и цену. Класс является потомком класса `Component<T>`, класс имеет тип `<T extends ICard>`

Конструктор:
`constructor(container: HTMLElement)` - в качестве контейнера принимает HTMLElement. Внутри вызывается родительский конструктор и происходит поиск необходимых элементов разметки.

Поля класса:
`protected _id: string = ''` - хранит идентификатор товара
`protected cardTitle: HTMLElement;` - хранит название товара
`protected cardPrice: HTMLElement;` - хранит цену товара

Методы класса:
`set id(value: string);` - установление идентификатора товара
`set title(value: string);` - установление наименования товара
`set price(value: string | null);` - установление стоимости товара


#### Интерфейс ICardCatalog extends ICard:
Поля интерфейса:
`category: string;` - категория, к оторой относится товар
`image: string;` - адрес изображения
`imageAlt?: string;` - описание изображения

#### Класс CardCatalog
Класс описывает карточку в каталоге товаров на главной странице. Класс является потомком класса `ICard`

Контруктор:
`constructor(container: HTMLElement, protected events: IEvents)` - контруктор принимает в качестве контейнера HTMLElement с классом `gallery__item` и событие для кнопки открытия карточки. Вся карточка является кнопкой. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки и устанавливается слушатель события клика на весь контейнер, который генерирует событие выбора карточки товара. 

Поля класса:
`protected cardCategory: HTMLElement;` - хранит категорию товара
`protected imageElemen: HTMLImageElement;` - хранит элемент изображения

Методы класса:
`set category(value: string);` - установление категории товара
`set image(value: string);` - установление адреса изображения
`set imageAlt(value: string);` - установление описания изображения.


#### Интерфейс ICardPreview extends ICard:
Поля интерфейса:
`category: string;` - категория, к оторой относится товар
`image: string;` - адрес изображения
`imageAlt?: string;` - описание изображения
`description: string;` - описание товара

#### Класс FullCard
Класс описывает карточку с полной информацией о товаре. Класс является потомком класса `Card`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - контруктор принимает в качестве контейнера HTMLElement с классом `card_full` и событие изменения состояния карточки товара в корзину (кнопка добавляет товар в корзину, если его еще нет, и удаляет, если он уже есть.). Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки и устанавливается слушатель клика на кнопку, который генерирует событие нажатия на кнопку карточки (добавления или удаления товара в корзину) и событие изменения внешнего вида кнопки.

Поля класса:
`protected cardCategory: HTMLElement;` - хранит категорию, к которой относится товар
`protected cardImage: HTMLImageElement;` - хранит элемент изображения
`protected cardDescription: HTMLElement;` - хранит описание товара
`protected cardButton: HTMLButtonElement;` - хранит кнопку добавления и удаления товара

Методы класса:
`set price(value: number | null)` - переопределяется метод родительского класса. К общему функционалу добавляется изменение состояния кнопки, если цена придет `null`, то кнопка карточки блокируется и меняет текст.
`set category(value: string);` - установление категории для товара
`set image(value: string);` - установление адреса изображения
`set imageAlt(value: string)` - установление описания изображения
`set description(value: string)` - установление описания товара
`set buttonChange(value: boolean)` - в качестве значения приходит наличие товара в корзине, в зависимости от этого меняется текст на кнопке


#### Интерфейс ICardCart extends ICard:
Поля интерфейса:
`index: number;` - номер товара в списке

#### Класс CardCart
Класс описывает карточку внутри корзины товаров. Такая карточка является элементом списка. Класс является потомком класса `Card`

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - контруктор принимает в качестве контейнера HTMLElement с классами `basket__item card_compact` и событие удаления товара из корзины. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки и устанавливается слушатель события клика на кнопку удаления товара, который генерирует событие удаления карточки товара из компонента корзины.

Поля класса:
`protected basketItemIndex: HTMLElement`- номер товара в списке элементов
`protected deleteButton: HTMLButtonElement` - кнопка удаления товара из корзины

Методы класса:
`set Index(value: number);` - установить индекс товара в списке


#### Интерфейс ICartComponentsData:
Поля интерфейса:
`lists: HTMLElement[]` - список товаров в корзине
`totalAmount: number` - общая стоимость товаров в корзине
`disabled: boolean;`- состояние кнопки

#### Класс CartComponent
Класс описывает компонент корзины товаров.

Конструктор:
`constructor(container: HTMLElement, protected events: IEvents)` - контруктор принимает в качестве контейнера HTMLElement с классом `basket` и событие для создания заказа. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки и устанавливается слушатель события клика на кнопку создания заказа, который генерирует событие для перехода к формам создания заказа.

Поля класса:
`protected itemLis: HTMLUListElement` - хранит список товаров
`protected totalPriceElement: HTMLElement` - хранит общую стоимость всех товаров
`protected orderButton: HTMLButtonElement` - кнопка для создания заказа

Методы класса:
`set lists(items: HTMLElement[])` - установление списка товаров, если список пустой, нопка оформления заказа блокируется
`set totalAmount(value: number)` - метод для установления общей стоимости товаров. Общая стоимость приходит из класса `Cart` при вызове `render`(передается в параметре)
`set disabled(state: boolean)`- устанавливает булево значение для блокировки кнопки


#### Компоненты формы создания заказа
При оформлении заказа покупателю нужно заполнить две формы. Для форм создан базовый класс `Form`, который описывает в себе общий функционал двух классов.

#### Интерфейс IForm
Поля интерфейса:
`error: string` - сообщение об ошибке валидации

#### Класс Form
Класс, описывающий базовый функционал формы: валидацию, показ ошибок и отправку данных. Класс является потомком класса `Component<T>`, класс имеет тип `<T extends IForm>`

Конструктор:
`constructor(container: HTMLFormElement)` - конструктор принимает в качестве контейнера элемент формы. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки.

Поля класса:
`protected form: HTMLFormElement;` - элемент формы
`protected submitButton: HTMLButtonElement` - кнопка отправки данных
`protected errorElement: HTMLElement` - элемент с сообщением об ошибке

Методы класса:
`set error(value: string)` - установление текста ошибки
`showError(errors: string[]): void` - принимает массив ошибок, объединяет их в строку, передает методу `set error` и меняет отображение кнопки (заблокирована или нет)


#### Интерфейс IOrderFormData extends IForm:
Поля интерфейса:
`paymentMethod: TPayment` - способ оплаты, тип `TPayment` может иметь значения `'card' | 'cash' |''`
`address: string` - адрес доставки

#### Класс OrderForm
Класс описывает форму оформления заказа, которая требует введения адреса доставки и выбор способа оплаты. Данный класс является потомком класса `Form`. 

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents)` - конструктор принимает в качестве контейнера элемент формы и событие для отправки данных на сервер. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки. Устанавливаются слушатели событий: на элементы кнопок выбора оплаты - `click` (вызывается событие изменения данных покупателя); на поле ввода адреса - `input`(вызывается событие изменения данных покупателя); на контейнер - `submit`(вызывается событие отправки формы). Кнопка отправки изначально заблокирована.

Поля класса:
`protected cardButton: HTMLButtonElement` - кнопка выбора метода оплаты `card`
`protected cashButton: HTMLButtonElement` - кнопка выбора оплаты `cash`
`protected addressInput: HTMLInputElement` - поле ввода адреса доставки
`protected _paymentMethod: TPayment = ''` - хранит выбранный метод способа оплаты, по-умолчанию не выбран => кнопка отправки данных заблокирована

Методы класса:
`set paymentMethod(value: TPayment)` - меняет наличие класса `button_alt-active` у кнопки в зависимости от метода оплаты
`set address(value: string)` - установление адреса доставки

#### Интерфейс IContactsFormData extends IForm:
Поля интерфейса:
`email: string` - адрес электронной почты
`phoneNumber: string` - номер телефона

#### Класс ContactsForm
Класс описывает форму оформления заказа, которая требует ввести номер телефона и адрес электронной почты. Данный класс является потомком класса `Form`.

Конструктор:
`constructor(container: HTMLFormElement, protected events: IEvents)`- конструктор принимает в качестве контейнера элемент формы и событие для отправки данных на сервер. Внутри конструктора вызывается родительский конструктор, происходит поиск необходимых элементов разметки. Устанавливаются слушатели событий: на поля ввода данных - `input` (вызывается событие изменения данных покупателя); на контейнер - `submit` (вызывается событие отправки формы). Кнопка отправки изначально заблокирована.

Поля класса:
`protected inputEmailElement: HTMLInputElement` - хранит адрес электронной почты
`protected inputPhoneNumberElement: HTMLInputElement` - хранит номер телефона

Методы класса:
`set email(value: string)` - установление адреса электронной почты
`set phone(value: string)` - установление номера телефона

### Presenter приложения
Презентер реализован функцией `init()`, внутри которой происходят подписки на события и описывается логика приложения.
Функция описана в файле `main.ts`, расположенном в папке `src`

#### Описание событий

`'order:updated'`- отслеживает изменения данных покупатиля и проверка данных валидатором форм

`'cart:change'` - отвечает за перерисовка компонентов корзины. Изменяется список товаров, общая сумма, счетчик корзины и состояние кнопки оформления заказа.

`'basket:cardDelete'` - отвечает за удаление товара из списка корзины в окне с корзиной.

`'productsCatalog:addProducts'` - отвечает за получение массива товаров из данных модели каталога, формируются карточки товаров и отрисовывается главная страница с карточками.

`'cart:open'` - отрисовывает корзину товаров в модальном окне.

`'cart:makeOrder'` - открывает форму с подтверждением заказа, заполнением данных покупателя и валиадция форм.

`'buyer:change'` - фиксирует изменение данных покупателя и вызывает метод модели данных для обновления имеющихся данных на новые.

`'order:update'` - получает данные об ошибках при валидации данных пользователя и отдает нужные ошибки формам создания заказа.

`'formOrder:submit'` - модальное окно создания заказа

`'formContacts:submit'` - инициирует валидацию данных в формах. Если ошибок нет, то заказ сичтается успешно созданным, и иницируется окошко об успешном создании заказа.

`'modal:close'` - закрвыает все модальные окна, вне зависимости от их содержимого
